import { User } from './user.meta';
export declare class UserController {
    getUser(id: number, name: string, all: object, uid: number): Promise<User>;
    searchUser(user: User): Promise<User>;
    addUser(user: User): Promise<any>;
}
